{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amiga-re.invalid/schemas/operations/v1/operations/analysis.code.globals.request.schema.json",
  "title": "analysis.code.globals request",
  "description": "Map the base-relative, absolute, and derived accesses a hunk's code makes, writing nothing. `read_only`.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "operation",
    "arguments"
  ],
  "properties": {
    "operation": {
      "const": "analysis.code.globals"
    },
    "arguments": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source"
      ],
      "properties": {
        "source": {
          "$ref": "https://amiga-re.invalid/schemas/operations/v1/common.schema.json#/$defs/sourceLocator"
        },
        "hunk": {
          "description": "CODE hunk to analyze.",
          "type": "integer",
          "minimum": 0,
          "default": 0
        },
        "entries": {
          "description": "Where the traversal starts, as hunk offsets. Defaults to offset 0.",
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0,
            "maximum": 4294967295
          }
        },
        "origin": {
          "description": "The address the hunk is mapped at. Absolute accesses cannot be identified without it — \"inside the image\" has no meaning until the image has a place — so that list is empty rather than guessed at.",
          "type": "integer",
          "minimum": 0,
          "maximum": 4294967295
        },
        "base_register": {
          "description": "Address register the small-data convention uses. A5 by convention, and the convention is what makes a displacement from it a global rather than an arbitrary offset from an arbitrary pointer.",
          "type": "integer",
          "minimum": 0,
          "maximum": 7,
          "default": 5
        },
        "derived": {
          "description": "Also resolve indirect accesses through propagated address registers. Off by default: it is an extra pass over straight-line code, and nobody should pay for it unless they asked.",
          "type": "boolean",
          "default": false
        },
        "maximum_accesses": {
          "$ref": "https://amiga-re.invalid/schemas/operations/v1/common.schema.json#/$defs/resultCap"
        },
        "maximum_input_bytes": {
          "$ref": "https://amiga-re.invalid/schemas/operations/v1/common.schema.json#/$defs/maximumInputBytes",
          "default": 67108864
        }
      }
    }
  },
  "examples": [
    {
      "operation": "analysis.code.globals",
      "arguments": {
        "source": {
          "kind": "file",
          "path": "extracted/game"
        },
        "origin": 65536,
        "derived": true
      }
    }
  ]
}
