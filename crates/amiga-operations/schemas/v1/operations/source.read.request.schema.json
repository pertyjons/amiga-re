{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amiga-re.invalid/schemas/operations/v1/operations/source.read.request.schema.json",
  "title": "source.read request",
  "description": "Read a window of a source's bytes, with the relocations inside it. Writes nothing. `read_only`.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "operation",
    "arguments"
  ],
  "properties": {
    "operation": {
      "const": "source.read"
    },
    "arguments": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source"
      ],
      "properties": {
        "source": {
          "$ref": "https://amiga-re.invalid/schemas/operations/v1/common.schema.json#/$defs/sourceLocator"
        },
        "hunk": {
          "description": "Read out of this hunk's bytes rather than the whole file. Naming one does the thing a raw file read cannot: it reports which offsets inside the window a relocation sits at. Omitted, the file is never parsed \u2014 a data file is an ordinary thing to look at.",
          "type": "integer",
          "minimum": 0
        },
        "offset": {
          "description": "Where the window starts, in whichever frame `hunk` selected: within that hunk's bytes when one is named, and within the file otherwise.",
          "type": "integer",
          "minimum": 0,
          "maximum": 4294967295,
          "default": 0
        },
        "length": {
          "description": "How many bytes the window holds. A window running past the end is reported short rather than refused \u2014 the tail of a file is an ordinary place to look, and refusing would make the last screen of every file unviewable.",
          "type": "integer",
          "minimum": 1,
          "maximum": 4294967295,
          "default": 256
        },
        "maximum_input_bytes": {
          "$ref": "https://amiga-re.invalid/schemas/operations/v1/common.schema.json#/$defs/maximumInputBytes",
          "default": 67108864
        }
      }
    }
  },
  "examples": [
    {
      "operation": "source.read",
      "arguments": {
        "source": {
          "kind": "file",
          "path": "extracted/game"
        },
        "hunk": 0,
        "offset": 256,
        "length": 128
      }
    }
  ]
}
