# amiga-re wishlist

Capabilities requested by downstream preservation projects that do not exist
yet. This is a lightweight backlog, not a committed roadmap. Completed requests
are removed; Git history records them.

Keep requests game-agnostic. Each entry states a priority, requester, likely
target, status, and purpose. When implementation exposes a concrete defect or
design task, record the actionable detail in `TODO.md` rather than duplicating
it here.

## Wishlist

No outstanding requests.
